# Template for Environment Variables

# Frontend Environment Variables (development only)
# File: frontend/.env
REACT_APP_BACKEND_URL=""

# Backend Environment Variables (for Netlify Functions)
# Add these in Netlify Site Settings > Environment Variables:

# MongoDB Atlas Connection String
MONGO_URL="mongodb+srv://username:password@cluster.mongodb.net/"

# Database Name
DB_NAME="your_database_name"

# Instructions:
# 1. For local development, you can use the frontend/.env file
# 2. For production on Netlify, add MONGO_URL and DB_NAME in Netlify dashboard
# 3. Never commit actual credentials to Git
# 4. Use MongoDB Atlas for cloud database (free tier available)